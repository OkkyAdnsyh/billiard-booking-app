import React from 'react';
import styles from '@/styles/components/blocks/Order/tableOrder.module.scss';

const TableOrder = () => {
  return (
    <>
      <section className={`${styles.container}`}>
        
      </section>
    </>
  )
}

export default TableOrder